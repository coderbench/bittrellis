schema: bittrellis/precision-manifest@1
track: HPC-01
name: V9-gdn-q4k-mlp-q4k
description: >
  V4 and V6 together. Interaction test: if the KL increase of V9 differs materially from the sum
  of V4's and V6's increases, module effects are not additive and search must model interactions.
default: NVFP4
rules:
  - match: "L*.gdn.*"
    precision: Q4_K
  - match: "L*.mlp"
    precision: Q4_K
