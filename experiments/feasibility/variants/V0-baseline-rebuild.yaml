schema: bittrellis/precision-manifest@1
track: HPC-01
name: V0-baseline-rebuild
description: >
  The baseline precision map rebuilt from the manifest (NVFP4 everywhere, baseline bytes).
  Must reproduce R0's tensors byte for byte: proves the builder adds nothing.
default: NVFP4
