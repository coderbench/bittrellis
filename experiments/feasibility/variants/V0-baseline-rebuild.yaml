schema: bittrellis/manifest@2
track: HPC-01
name: V0-baseline-rebuild
description: >
  The baseline precision map rebuilt from the manifest (NVFP4 everywhere, baseline bytes).
  Must reproduce the shipped checkpoint's tensors byte for byte: proves the builder adds nothing.
default: NVFP4
